<template name="node-header">
  <view class="u-flex node-header__title">
    <view>
      <view class="node-header__full">{{full_name}}</view>
    </view>
    <image class="node-header__avatar" src="{{image_url}}" mode="aspectFill" wx:if="{{image_url !== undefined}}" />
  </view>
</template>
